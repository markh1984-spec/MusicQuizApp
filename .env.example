# Copy this file to ".env" and fill in the bits you have.
#
#   cp .env.example .env
#
# The app reads it automatically when you run npm start, npm run generate:quiz
# and so on. You do not need any of these to run a quiz — they are only for
# the AI generation and the Spotify playlists.
#
# .env is in .gitignore, so it never gets committed. Keep it that way: these
# are passwords.
#
# On your host (Render) you do NOT use this file — you paste the same
# name/value pairs into the dashboard under Environment.


# The password for your control view, console and editor.
# Leave it out and the app invents one and prints it on startup.
HOST_KEY=change-me-to-something-you-can-type-in-the-dark

# Your name on it. Appears on the console, the control view, the editor, the
# players' phones and the corner of the big screen. Change it here and it
# changes everywhere.
# BRAND_NAME=Mark's Music Madness


# --- AI generation -----------------------------------------------------
# Writes quizzes and picks bingo tracks. From console.anthropic.com.
# ANTHROPIC_API_KEY=sk-ant-...


# --- Round 2 portraits -------------------------------------------------
# Draws the "whose face is this?" pictures. Anthropic has no image API, so
# this has to be a separate account whichever one you use.
#
# Google is the one in use — an AI Studio key, made against your existing
# Google Cloud project, from https://aistudio.google.com/apikey. Billing has
# to be on for that project or every picture comes back refused.
# GOOGLE_API_KEY=AIza...
#
# OpenAI still works and is used if it is the only one set, but it is dearer.
# OPENAI_API_KEY=sk-...


# --- Keeping your packs (important on a free host) ----------------------
# Your host wipes its filesystem every time the app restarts, so a quiz
# generated on the live site is temporary. With these two set, every pack you
# generate or edit is filed into your GitHub repository automatically, which
# is the only permanent place in the setup.
#
# The commits say [skip render], so filing a pack does NOT restart the app.
#
# Make a token at https://github.com/settings/personal-access-tokens/new
#   - Repository access: only your MusicQuizApp repo
#   - Permissions: Contents = Read and write
# GITHUB_TOKEN=github_pat_...
# GITHUB_REPO=markh1984-spec/MusicQuizApp
# GITHUB_BRANCH=MusicQuizApp


# --- Grouping your Spotify playlists ------------------------------------
# Spotify's API cannot create folders or move playlists into them — that is a
# gap in their API, not something the app can work round. So every playlist it
# makes is named with this prefix, which means they sort together in your
# sidebar and you can drag the lot into a folder in one go.
# Set it to an empty value to turn the prefix off.
# SPOTIFY_PLAYLIST_PREFIX=Quiz Intros


# --- Spotify playlists -------------------------------------------------
# Builds the playlist your DJ app plays a bingo round from.
# Run `npm run spotify:login` and it prints all three of these for you.
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
# SPOTIFY_REFRESH_TOKEN=
